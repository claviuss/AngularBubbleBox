import { BubbleBoxDirective } from './bubble-box.directive';

describe('BubbleBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new BubbleBoxDirective();
    expect(directive).toBeTruthy();
  });
});
